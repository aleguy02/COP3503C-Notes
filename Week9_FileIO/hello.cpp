#include <iostream>

/* To compile this from the command line, open a terminal in CLion (or any terminal)
 * First, check that you have g++ installed with `g++ --version`.
 * Then, run `g++ -o my_executable_file hello.cpp` You should see a my_executable_file or
 * my_executable_file.exe file show up in your file tree.
 * Note: my_executable_file is just the name of the file. It can be whatever you want, sometimes it is `out` or `out.exe`
 *
 * To run your executable, run `./my_executable_file`.
 */
int main() {
  std::cout << "Hi class" << std::endl;
}